let greetings: string = "Hello Shubham";

let mynum = 6;
// greetings.toLowercase(); // TS will throw an error because C should be in uppercase
// mynum.toLowerCase(); // TS will throw an error because mynum is a number not a string

console.log(greetings);

export {}; // to remove the error temporarily
