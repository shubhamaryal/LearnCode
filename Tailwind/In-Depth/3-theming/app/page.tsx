import Hero from "@/componenets/hero";

export default function Home() {
  return (
    <div className="flex h-screen justify-center bg-neutral-900">
      <Hero />
    </div>
  );
}

// There is a library which automatically sorts the tailwind classes, we can install it and use it for professional classes sorting
