import React from "react";

const Cc = (prop) => {
  return (
    <>

      <div>{prop.username}</div>
      <div>{prop.username}</div>
      <div>{prop.username}</div>
      <div>{prop.username}</div>
      <div>{prop.username}</div>
      <div>{prop.username}</div>
    </>
  );
};

export default Cc;
